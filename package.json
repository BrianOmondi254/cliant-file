{
  "name": "cliant-mobile",
  "version": "1.0.0",
  "description": "Mobile banking application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "devStart": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "capacitor:sync": "npx cap sync",
    "capacitor:open": "npx cap open android",
    "build:android": "npx cap sync && npx cap copy android && cd android && ./gradlew assembleDebug"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "bcrypt": "^6.0.0",
    "claude": "^0.1.1",
    "ejs": "^3.1.10",
    "express": "^5.2.1",
    "express-flash": "^0.0.2",
    "express-session": "^1.18.2",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0",
    "session-file-store": "^1.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "dotenv": "^17.2.3",
    "nodemon": "^3.1.11"
  }
}

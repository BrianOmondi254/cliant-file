# Plan: Update Member Verification Display

## Goal
Update the member verification display in `new_group.ejs` to match the provided template, while keeping all JavaScript functions, element IDs, and functionality unchanged.

## Current State Analysis
The current verification display in `buildVerificationCard` function shows:
- Member name prominently
- Role/title
- Member number (if provided)
- Phone number
- Index and status badges

## Target Template Structure
The provided template shows:
1. **Header Box** (regional & group info):
   - Regional Block
   - Group No, Group Type
   - Certificate, Agent Name, Phone

2. **Member Details**:
   - Member No
   - "Verified Successfully ✔" (green success message)
   - Name
   - ID Number
   - Status

## Required Changes

### 1. Add Group Info Header Box
- Add a new section at the top of verification step showing group/regional info
- Display: Regional Block, Group No, Group Type, Certificate, Agent Name, Phone
- Use data from `groupData` and `groupType` variables

### 2. Update Member Card Structure
- Restructure `buildVerificationCard` function HTML output to match template
- Show Member No first
- Add green "Verified Successfully ✔" message below Member No
- Then Name, ID Number, Status
- Keep all existing parameters and logic intact

### 3. Update CSS Styling
- Add styles for `.header-box` (light background, padding, border-radius)
- Style `.row` for two-column layout
- Style `.label` (bold, gray) and `.value` (dark)
- Style `.success` (green, centered, bold)
- Ensure responsive design

### 4. Preserve Functionality
- Keep all element IDs unchanged
- Keep all JavaScript functions unchanged
- Keep verification logic and data flow intact
- Only change HTML structure and CSS classes

## Implementation Steps
1. Update `step4-verify` HTML to include header box
2. Modify `buildVerificationCard` function to return new HTML structure
3. Add required CSS classes
4. Test that all IDs and functions work correctly

## Files to Modify
- `views/agent/new_group.ejs` (only HTML structure and CSS, no JS changes)

## Testing
- Verify member verification still works
- Check that all data displays correctly
- Ensure mobile responsiveness
- Confirm submit button logic unchanged</content>
<parameter name="filePath">C:\Users\Brian\Desktop\cliant\.kilo\plans\1775743853599-misty-lagoon.md